diff --git a/frontend/src/agent/AgentPage.tsx b/frontend/src/agent/AgentPage.tsx
index dbcd910..526770d 100644
--- a/frontend/src/agent/AgentPage.tsx
+++ b/frontend/src/agent/AgentPage.tsx
@@ -296,7 +296,7 @@ export function AgentPage() {
               {tab.label}
             </div>
           ))}
-          {agent.telegram_enabled && (
+          {(
             <div
               onClick={() => setActiveTab('telegram')}
               style={{
diff --git a/frontend/src/index.css b/frontend/src/index.css
index ccf0d24..a04adb1 100644
--- a/frontend/src/index.css
+++ b/frontend/src/index.css
@@ -75,3 +75,27 @@ input:focus, textarea:focus, select:focus {
 @keyframes blink {
   50% { opacity: 0; }
 }
+
+.agent-mark-tooltip {
+  position: absolute;
+  bottom: calc(100% + 8px);
+  left: 50%;
+  transform: translateX(-50%) translateY(4px);
+  background: #1a1f27;
+  color: #EDF0F4;
+  font-family: 'Inter Tight', system-ui, sans-serif;
+  font-size: 12px;
+  font-weight: 500;
+  padding: 5px 10px;
+  border-radius: 6px;
+  white-space: nowrap;
+  pointer-events: none;
+  opacity: 0;
+  transition: opacity 0.15s, transform 0.15s;
+  border: 1px solid rgba(230, 235, 242, 0.1);
+}
+
+.agent-mark-hover:hover .agent-mark-tooltip {
+  opacity: 1;
+  transform: translateX(-50%) translateY(0);
+}
diff --git a/frontend/src/login/LoginPage.tsx b/frontend/src/login/LoginPage.tsx
index 62d4852..975cbc2 100644
--- a/frontend/src/login/LoginPage.tsx
+++ b/frontend/src/login/LoginPage.tsx
@@ -44,37 +44,45 @@ export function LoginPage() {
       {/* Left marketing panel */}
       <div style={{
         flex: 1.1, borderRight: '1px solid rgba(230,235,242,0.07)',
-        padding: '44px 48px', display: 'flex', flexDirection: 'column',
-        justifyContent: 'space-between', position: 'relative', zIndex: 2,
+        padding: '52px 56px', display: 'flex', flexDirection: 'column',
+        justifyContent: 'flex-end', gap: 40,
+        paddingBottom: 80, position: 'relative', zIndex: 2,
       }}>
-        <div style={{ color: 'var(--color-ch-accent, #C8D1D9)' }}>
-          <IconWordmark height={26} color="currentColor" />
-        </div>
-
         <div>
-          <div style={mono(10, 'rgba(237,240,244,0.38)')}>AI Agents for small business</div>
+          <div style={{ color: 'var(--color-ch-accent, #C8D1D9)', marginBottom: 28 }}>
+            <IconWordmark height={56} color="currentColor" />
+          </div>
           <h1 style={{
             fontFamily: "'Fraunces', Georgia, serif",
-            fontSize: 60, fontWeight: 400, letterSpacing: '-0.028em',
-            lineHeight: 1.02, margin: '16px 0 0',
+            fontSize: 54, fontWeight: 400, letterSpacing: '-0.025em',
+            lineHeight: 1.1, margin: 0,
           }}>
-            Hire a team<br />of <span style={{ fontStyle: 'italic', color: '#D4A85A' }}>agents</span>,
-            <br />not just <span style={{ color: 'rgba(237,240,244,0.62)' }}>a chatbot.</span>
+            Hire a team of agents,<br />not just a&nbsp;chatbot.
           </h1>
-          <div style={{
-            fontFamily: "'Fraunces', Georgia, serif",
-            fontSize: 18, fontStyle: 'italic',
-            color: 'rgba(237,240,244,0.62)', marginTop: 22,
-            maxWidth: 440, lineHeight: 1.5,
+          <p style={{
+            fontSize: 16, color: 'rgba(237,240,244,0.52)',
+            marginTop: 20, maxWidth: 420, lineHeight: 1.6,
           }}>
             Commission a personal assistant, an AP clerk, a sales rep.
             They learn your business, handle the tedious work, and stay quietly at their desks.
-          </div>
+          </p>
         </div>
 
         <div style={{ display: 'flex', gap: 12 }}>
-          {['C', 'H', 'A', 'T', 'T', 'Y'].map((l, i) => (
-            <AgentMark key={i} letter={l} size={36} />
+          {([
+            { letter: 'C', name: 'Clara' },
+            { letter: 'H', name: 'Harper' },
+            { letter: 'A', name: 'Arlo' },
+            { letter: 'T', name: 'Tessa' },
+            { letter: 'T', name: 'Tobias' },
+            { letter: 'Y', name: 'Yuki' },
+          ] as const).map((agent, i) => (
+            <div key={i} style={{ position: 'relative' }} className="agent-mark-hover">
+              <AgentMark letter={agent.letter} size={36} />
+              <div className="agent-mark-tooltip">
+                {agent.name}
+              </div>
+            </div>
           ))}
         </div>
       </div>
diff --git a/frontend/src/onboarding/steps/CompletionStep.tsx b/frontend/src/onboarding/steps/CompletionStep.tsx
index 6492b9e..c75c1c3 100644
--- a/frontend/src/onboarding/steps/CompletionStep.tsx
+++ b/frontend/src/onboarding/steps/CompletionStep.tsx
@@ -19,6 +19,8 @@ const INTEGRATION_NAMES: Record<string, string> = {
   quickbooks: 'QuickBooks',
   bamboohr: 'BambooHR',
   crm_lite: 'CRM',
+  telegram: 'Telegram',
+  whatsapp: 'WhatsApp',
 };
 
 export function CompletionStep({ result, onComplete }: Props) {
